// Imports
import { StyleSheet } from 'react-native'

// UI Imports
// import { white, grey } from '../../ui/common/colors'
// import { blockMargin, blockPadding, itemRadius } from '../../ui/common/responsive'

// Styles
export default StyleSheet.create({
  container: {
    flex: 1
  }
})
