// Component Styles
const styles = (theme) => ({
  root: {
    marginBottom: theme.spacing(3),
  },

  content: {
    paddingBottom: 0,
  },

  note: {
    whiteSpace: 'pre-line',
  },
})

export default styles
